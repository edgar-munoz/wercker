{
  "swagger": "2.0",
  "info": {
    "title": "rdd.proto",
    "version": "version not set"
  },
  "schemes": [
    "http",
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {},
  "definitions": {
    "rddActionResponse": {
      "type": "object",
      "properties": {
        "success": {
          "type": "boolean",
          "format": "boolean"
        }
      }
    },
    "rddDaemonState": {
      "type": "string",
      "enum": [
        "requested",
        "provisioning",
        "provisioned",
        "deprovisioning",
        "error"
      ],
      "default": "requested",
      "title": "- requested: requested means the provisioning of the specified remote docker daemon has been requested but has not started\n - provisioning: provisioning means the provisioning of the specified remote docker daemon is in progress\n - provisioned: provisioned means the specified remote docker daemon has been provisioned\n - deprovisioning: deprovisioning means the specified remote docker daemon is being deprovisioned\n - error: error means that the specified remote docker daemon could not be provisioned"
    },
    "rddRDDDeprovisionResponse": {
      "type": "object"
    },
    "rddRDDProvisionResponse": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        }
      }
    },
    "rddRDDStatusResponse": {
      "type": "object",
      "properties": {
        "state": {
          "$ref": "#/definitions/rddDaemonState"
        },
        "runID": {
          "type": "string"
        },
        "creationDate": {
          "type": "string",
          "format": "date-time"
        },
        "updateDate": {
          "type": "string",
          "format": "date-time"
        },
        "URL": {
          "type": "string"
        }
      }
    }
  }
}
